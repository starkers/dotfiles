# vi: ft=gitconfig
#
# examples for signing with another key on a specific github org
[user]
  name = David Stark
  email = david.stark@acmecorp.com
  signingKey = B1212121212BBBBBBBBBBBBBBBBBBB
  ; signingkey = ~/.ssh/id_acmecorp

; [init]
;   defaultBranch = master
; [push]
;   default = simple
; [github]
;   user = myaccountname
; # keep some https credential cache
; [credential]
;   helper = cache --timeout=86400
; [core]
;   excludesfile = /home/starkers/.git_global_ignore
; [url "git@github.com:"]
;   insteadOf = https://github.com/
; [gpg]
;   program = gpg
; [commit]
;   gpgSign = true
; [tag]
;   forceSignAnnotated = false
