#!/usr/bin/env pyston

## pip install pyperclip
import pyperclip

## grab contents of clipboard
obj = pyperclip.paste()

## split it by "\\n" dividers
foo = obj.split(sep="\\n")

for i in foo:
    print(i)
    # i = i.split(sep="\n")
    # for j in i:
    #     print(j)
