--- 

- defaults:
    link:
      create: true
      relink: true
      force: true
      glob: true

- link:
    ~/.config: 'config/*'
      # path: config/**
      # relink: true
      # force: true
      # create: true

